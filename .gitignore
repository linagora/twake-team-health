node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local scratch (rosters, logs, exports)
scratchpad/

# Playwright MCP snapshots (capture live page data)
.playwright-mcp/

# Per-machine compose overrides (ports, mounts, local env)
docker-compose.override.yml

# Local working notes, not deliverables
docs/superpowers/
